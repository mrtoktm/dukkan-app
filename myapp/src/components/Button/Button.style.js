import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        padding: 10,
        margin: 10,
        backgroundColor: 'black',
        borderRadius: 5,
        alignItems: 'center',
    },
    title:{
        fontWeight: 'bold',
        fontSize: 15,
        color: 'darkorange',
    },
});